# Required environment variables for Supabase Google authentication:
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# No additional environment variables are needed for Google OAuth with Supabase
# Just make sure you've configured the OAuth provider in the Supabase dashboard
# and set up the correct redirect URI: https://your-domain.com/auth/callback
